import { FaPepperHot as icon } from 'react-icons/fa';

export default {
  // Computer Name, used in for example 🔼
  name: 'topping',
  type: 'document',
  icon,
  fields: [
    {
      name: 'name',
      type: 'string',
    },
  ],
};
